#include <stdio.h>

int main() {
        int num[10];
        int par[10];
        int impar[10];

        for (int i = 0; i < 10; i++)
        {
            printf("Digite uma letra: ");
            scanf(" %c", &num[i]);
            
            for (int z = 0; z < count; z++)
            {
                /* code */
            }
        }

        for (int j = 0; j < 10; j++)
        {
           printf("%d. %d\n", j + 1, letra[j]);
        }

    return 0;
}